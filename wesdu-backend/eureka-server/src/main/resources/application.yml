server:
  port: 8888

spring:
  application:
    name: eureka-server
    version: 1.0.0

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8888/eureka
